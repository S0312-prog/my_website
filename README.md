# Soumya Shree S – personal website

Personal research website (React + Vite + Tailwind), deployed on Netlify.

```bash
npm install
npm run dev     # local server on http://localhost:3000
npm run build   # production build in dist/
```
